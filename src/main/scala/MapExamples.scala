/**
 * Class for
 */
object MapExamples {

  def run() = {

    // Define a list of integers
    val integerList = List(1, 2, 3, 4, 5)

    // Q1. Make a new list, adding 1 to each element of 'integerList'


    // Q2. Make a new list, multiplying each element of 'integerList' by 5


    // Q3. Make a new list, converting integer to its string type


    val nameList = List("adam, john", "tom", "betty")


    // Q4. Make a new list where each name in 'nameList' is in upper case


    val nameIds = Map( "adam" -> 4, "john" -> 21, "tom" -> 2, "betty" -> 1, "sam" -> 9, "harry" -> 20 )

    // Q5. Using the above 'nameIds', make a new list having the ids of the names in 'nameList


  }
}
